export const selectProducts = (reduxState) => reduxState.products.allProducts;
export const selectLoading = (reduxState) => reduxState.products.loading;
export const selectProduct = (reduxState) => reduxState.products.product;
